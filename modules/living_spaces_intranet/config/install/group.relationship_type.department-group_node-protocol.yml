langcode: en
status: true
dependencies:
  config:
    - group.type.department
    - node.type.protocol
  module:
    - gnode
    - node
id: department-group_node-protocol
label: 'Department: Group node (Protocol)'
description: 'Adds <em class="placeholder">Protocol</em> content to groups both publicly and privately.'
group_type: department
content_plugin: 'group_node:protocol'
plugin_config:
  group_cardinality: 0
  entity_cardinality: 1
  use_creation_wizard: false
